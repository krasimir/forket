{
  "name": "forket-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "clean": "rm -rf ./dist/* && rm -rf ./build/*",
    "dev": "npm run clean && node ./scripts/dev.js"
  },
  "dependencies": {
    "forket": "0.8.10"
  },
  "engines": {
    "node": ">=14.8.0"
  }
}
